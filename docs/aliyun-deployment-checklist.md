# 阿里云部署信息填写清单

把下面内容按实际情况填写后发给我，我就可以继续帮你制定部署方案，或者直接开始准备部署脚本与上线步骤。

---

## 1. 部署目标

- 部署方式：
  - 正式生产：ECS + Nginx + RDS MySQL + OSS + HTTPS
- 这次希望我提供：
  - 远程协助实际部署

补充说明：

```text
请填写
```

---

## 2. 阿里云资源现状

### 2.1 ECS 服务器

- 是否已有 ECS：
  - 有

- 公网 IP：
- 操作系统：
- 地域：
- 服务器规格（例如 2核4G / 4核8G）：
- 系统盘大小：
- 数据盘大小：
- 安全组是否可修改：
  - [ ] 可以
  - [ ] 不确定
  - [ ] 不可以

### 2.2 数据库

- 是否已有阿里云 RDS MySQL：
  - 没有


### 2.3 对象存储 OSS

- 是否已有 OSS：
  - 没有


### 2.4 域名和证书

- 是否已有域名：
  - 有
- 主域名：helenspub.xyz
- 是否可配置 DNS：
  - 是
- 是否已有 SSL 证书：
  - 有
- 证书来源（阿里云/Let's Encrypt/其他）：阿里云

---

## 3. 计划开放的访问地址

这个项目目前包含以下入口，请勾选哪些要上线：

- 管理后台 `admin-web`
- 公开前台 `public-mobile`
- 员工端 `staff-mobile`

如果你已经想好了域名或子域名，请填写：

- 管理后台域名：helenspub.xyz/admin
- 公开前台域名：helenspub.xyz/menu
- 员工端域名：helenspub.xyz/staff

如果暂时没有域名，是否先用 IP 部署测试：

- 是

---

## 4. 服务器登录信息

我的服务器是阿里云轻量应用服务器，系统镜像使用的是宝塔Linux面板。
- 公网IP是：139.196.206.35
- SSH端口：22
- 用户名：root
- 密码：Xie18608525447

- 请注意： 因为我装了宝塔面板，请不要给我直接在系统底层安装 Nginx 或 Node.js 的命令。如果需要部署网站或应用，请告诉我如何在宝塔面板的终端或通过宝塔面板的图形界面来进行操作和配置。


说明：

```text
如果你不方便提供登录信息，也没关系，我可以改为给你完整的部署文档和脚本模板。
```

---

## 5. 数据库处理方式

- 这次是：
  - 全新空库部署
- 当前数据库位置：
- 当前数据库大小（大概即可）：40G
- 是否需要保留现有管理员账号、鸡尾酒数据、图片关联数据：
  - 是

如果要迁移数据，请补充：

- 是否可以导出 SQL：
  - 可以
- 是否已有备份文件：
  - 没有
- 备份文件路径：

---

## 6. 图片与上传文件处理方式

当前项目的图片上传是保存在服务器本地目录 `storage/uploads`。

请选择这次的方案：
- 使用服务器本地存储

如果使用本地存储，请确认：

- 是否接受以后更换服务器时需要迁移图片文件：
  - 是


---

## 7. 生产环境配置

请填写或确认以下生产环境变量：

- `NODE_ENV=production`
- `PORT=` 
- `DATABASE_URL=`
- `JWT_SECRET=`
- `JWT_EXPIRES_IN=` 
- `UPLOAD_DIR=` 
- `UPLOAD_BASE_URL=` 
- `DEFAULT_ADMIN_USERNAME=`
- `DEFAULT_ADMIN_PASSWORD=`

如果前端要独立配置 API 地址，也请填写：

- `VITE_API_BASE_URL=`
- `VITE_API_ORIGIN=`

---

## 8. 发布方式偏好

- 代码获取方式：
  - 服务器直接 `git clone`
- 是否已有 Git 仓库可供服务器拉取：
  - 有
- 如果有，请填写仓库地址：

是否接受使用以下运行方式：

- 后端：
  - [ ] PM2
  - [ ] Docker
  - [ ] systemd
- 前端静态资源：
  - [ ] Nginx
  - [ ] Docker

---

## 9. 安全与运维要求

- 是否需要 HTTPS：
  - [ ] 是
  - [ ] 否
- 是否需要限制后台访问：
  - [ ] 是
  - [ ] 否
- 是否需要定时备份数据库：
  - [ ] 是
  - [ ] 否
- 是否需要日志目录和日志轮转：
  - [ ] 是
  - [ ] 否
- 是否需要监控或报警：
  - [ ] 是
  - [ ] 否

补充要求：

```text
请填写
```

---

## 10. 备注与特殊限制

- 请注意： 因为我装了宝塔面板，请不要给我直接在系统底层安装 Nginx 或 Node.js 的命令。如果需要部署网站或应用，请告诉我如何在宝塔面板的终端或通过宝塔面板的图形界面来进行操作和配置。

请填写：

```text
请填写
```

---

## 11. 我已经根据项目确认到的现状

下面这些是我已经从代码里确认到的，方便你核对：

- 项目是 `pnpm workspace`
- 后端是 `NestJS + Prisma + MySQL`
- 前端有 3 个：
  - `admin-web`
  - `public-mobile`
  - `staff-mobile`
- 默认后端端口是 `3000`
- 当前上传目录是 `storage/uploads`
- 当前示例环境变量里包含：
  - `DATABASE_URL`
  - `JWT_SECRET`
  - `JWT_EXPIRES_IN`
  - `PORT`
  - `DEFAULT_ADMIN_USERNAME`
  - `DEFAULT_ADMIN_PASSWORD`
- 当前 `public-mobile` 和 `staff-mobile` 支持通过 `VITE_API_BASE_URL` 配置 API 地址
- 当前 `admin-web` 代码里 API 地址还是写死的，上线前建议我一并改成环境变量

---


