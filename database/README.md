# Database Assets

- `mysql-data/`: local Docker MySQL data directory
- `migrations/`: SQL snapshots or helper scripts when needed
- `schema/`: reference exports if later needed
- `seeds/`: extra seed assets
